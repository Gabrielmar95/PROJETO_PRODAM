# Retomada — atualizado 2026-06-10 (organização da raiz, Sessão 1 concluída)

> Histórico antigo preservado via git (`git log .continue-here.md`). Registros de 2026-05-31 e 28/05 seguem **abaixo** — as decisões SES/SUSAM (D1/D2/D3) continuam sendo pendência local viva.

## Sessão 2026-06-10 — Organização da raiz (Sessão 1 de 2) ✅ CONCLUÍDA

Raiz reduzida de ~130 arquivos soltos para 34 intencionais. 2 commits (pushed em 10/06 junto com `d5d4043d`):

| Commit | Conteúdo |
|---|---|
| `677f7b8` | Fase A — 40 deleções (lixo aprovado pelo Gabriel, sem quarentena) |
| `1511477` | Fase B — 135 files: 21 fichas DETRAN → `docs\DETRAN_TEMATICOS\` · 6 scripts → `scripts\` · 4 docs sessão → `relatorios\sessoes\` · 6 relatórios → `relatorios\` · 5 dados → `dados\` · kit → `docs\KIT_SETUP\` · 5 pastas legadas → `_ARQUIVO\` · manifesto |

Trilha de auditoria: `relatorios\ORGANIZACAO_RAIZ_20260610_MANIFESTO.json` (112 ações, MD5 pré-ação, commitado) + `.csv` gêmeo (no disco, gitignored por política `*.csv`). 0 PDFs tocados (`git log --diff-filter=D -- "*.pdf"` vazio). Scripts movidos foram ancorados com `ROOT = Path(__file__).resolve().parents[1]` e smoke-testados (69 devedores OK). Drift corrigido: `ses_reconciliacao_completa.py` e `reconciliacao_4_fontes.py` agora gravam em `dados\`/`relatorios\`, não na raiz.

### Próximos passos da retomada (ordem de prioridade)

1. ✅ **CONCLUÍDO 10/06** — comandos manuais dos `.db` executados pelo Gabriel: `prodam_BAK_ANTES_SYNC_20260420_164054.db` deletado, `prodam_BAK_REGRESSAO_20260507_163309.db` movido para `_ARQUIVO\`. Raiz só tem `prodam.db` (derivado legítimo).
2. ✅ **CONCLUÍDO 10/06** — push feito: `677f7b8` + `1511477` + `d5d4043d` em origin/main.
3. ✅ **CONCLUÍDO 10/06** — commit único `d5d4043d`: regeneração 10/06 (316 files) + este arquivo + move Rastreabilidade → `relatorios\` + 3 registros de sessão + 2 pastas `_OUT_sync` de 23/04. Fora dele (de propósito): Dashboard html (item 4) e `_ORGANIZADO_2026-06-10\` (79 violações de citação quebrariam o CI).
4. **PR #24 — conteúdo já incorporado na main** (`chore/retomada-2026-06-10` → `main`): criado manualmente pelo navegador em 10/06 durante outage da API do GitHub. Verificado via `git ls-remote` em 10/06: `refs/heads/main` = `refs/pull/24/head` = `68bea75f` — origin/main já contém todos os commits do PR (avanço por push direto, não merge pela UI). Quando a API voltar, rodar `gh pr view 24` para confirmar se o GitHub marcou o PR como merged ou se precisa fechar manualmente. URL: https://github.com/Gabrielmar95/PROJETO_PRODAM/pull/24
5. **`gh` CLI inoperante até a API voltar**: `api.github.com` (edge 4.228.31.149, São Paulo/Azure) com timeout TCP na porta 443 em 10/06 — git puro (push/pull) funciona normal. O aviso "token in keyring is invalid" do `gh auth status` é **inconclusivo durante o outage** (gh não alcança a API para validar). Quando `Test-NetConnection api.github.com -Port 443 -InformationLevel Quiet` retornar `True`, rodar `gh auth status`; só se ainda acusar token inválido → `gh auth refresh -h github.com`.
6. **Adiados na raiz até confirmação de envio à PRODAM**: `Relatorio_Quinzenal_PRODAM_2026-06-10.docx` + `Dashboard_PRODAM_Relatorio_Quinzenal_2026-06-10.html` (html segue untracked de propósito).
7. **`_ORGANIZADO_2026-06-10\`**: untracked de propósito — 79 violações de citação quebrariam o CI `test_projeto_inteiro_passa`. Só trackear depois de sanear as citações.
8. **Sessão 2 (NÃO executar sem pedido)**: `DETRAN_AUDITORIA_COMPLETA` do Desktop — 15.281 arquivos, 9,5 GB.

### Avisos conhecidos (não são bugs novos)

- pytest: 2 falhas **pré-existentes** — `test_exatamente_13_regras` (gerador emite 14 regras desde a Regra 14 DETRAN; teste desatualizado) e `test_projeto_inteiro_passa` (79 violações de citação, 100% em `_ORGANIZADO_2026-06-10\`, pasta não tocada). 157 passed no resto.
- `CLAUDEMD.bak.20260604_021606.md` citado no plano não existia em disco.
- `_ARCHIVE_CLAUDE` tinha 6 configs antigos → movida inteira para `_ARQUIVO\_ARCHIVE_CLAUDE` (não deletada).

---

## Estado atual (2026-05-31)

- **Git**: `main` = `claude/clever-bell-nhsDa` = `cdaa7b5` — **consolidação na main CONCLUÍDA** (PR #11 mergeado). Working tree limpo. CI verde: 136 testes, `validar_citacoes` exit 0, `compileall` exit 0.
- **Já na main** (não repetir): janelas 0–3; issues 08/09/10/12/15/17 + cascata Teori→Delgado fechadas; 11 Cat A (DRY brl/fmt_brl); hooks cloud SessionStart + PreCompact; validadores CI (citações + cron de prescrição).
- **Status do tracker `_QUESTOES_CRITICAS/`** (índice versionado):

  | Estado | Issues |
  |---|---|
  | ✅ fechadas | 08, 09, 10, 12, 15, 17, cascata Teori→Delgado, 11 (Cat A) |
  | ⏸ local-only (precisa PRODAM_DOCS/prodam.db) | 01, 02, 03, 06, 07, 14, 11 (Cat B/C) |
  | ⏸ opt-in (seu OK por item) | 16 §2/§3 (hooks/MCP/subagentes), 13 (skill run-prodam) |
  | 🔴 alertas de prescrição (cron) | GitHub #6/#7 — 23 prescritos + 1 crítico (ADS) = 24 em risco |

- **Pendências reais p/ Gabriel (local)**: decisões SES/SUSAM da Issue 03 (D1 valor real · D2 restaurar `profiles.json` · D3 validar 125 evidências SSP); TRD SES/SUSAM (item 1.6, após D1); pipelines ADS/FENIXSOFT; restauração `profiles.json` (R1).

## TL;DR

Em 1 sessão (28/05), fechei 3 commits que avançam Janela 2 item 2.4, Janela 1 item 1.2, e aplicam `/simplify` com 13 findings:

| Commit | Janela | Escopo |
|---|---|---|
| `f8290de` | 2.4 | float()→Decimal cirúrgico + DRY brl/fmt_brl em dossie + 8 testes + Issue 10 |
| `11d0c40` | 1.2 | Auditoria forense reversão drift 13/05 + Issue 03 com 3 decisões pendentes |
| `cb61891` | /simplify | 13 findings: regex→AST nos testes de regressão + 3 erros factuais nas issues |

**Validação**: 113 passed + 1 xfailed (xpass-on-Issue-10), ruff E9/F63/F7/F82 limpo, working tree limpo.

## 🚨 Bloqueio crítico descoberto

**SES/SUSAM**: TRD em disco usa R$ 4,78M (reconciliado 12/05); CSV atual diz R$ 14,75M (revertido 13/05 sem changelog).
**Item 1.6 do roadmap (envio TRD) BLOQUEADO** até Gabriel decidir qual valor é o real.

## Decisões pendentes — gate humano obrigatório

### Decisão 1 — SES/SUSAM val_exig real?

| Opção | val_exig | val_atualizado | fat_exig | Fundamento |
|---|---:|---:|---:|---|
| **A** (backup 12/05) | R$ 4.783.356,52 | R$ 8.230.061,40 | 82 | Cálculo fatura-a-fatura, tier1 Art. 206 §5º I CC + tier2 Decreto 20.910/1932 NE+2,5a, metodologia SELIC documentada (`scripts/ad_hoc/gerar_memorial_preliminar_ses.py`) |
| **B** (CSV atual) | R$ 14.748.048,96 | R$ 43.921.619,17 | 144 | Soma bruta SPCF, sem reconciliação metodológica |

**Recomendação minha: A** — única defensável adversarialmente. TRD em `DOCUMENTOS_GERADOS/SES_SUSAM/TRD_SES_SUSAM_2026-05-12.docx` foi feito com base em A.

### Decisão 2 — Restaurar profiles.json?

| Opção | Ação | Quando |
|---|---|---|
| **R1** Restauração total | `cp _BACKUPS/profiles_PRE-REVERSAO-DRIFT-20260513-155115.json PRODAM_DOCS/profiles.json` + regerar CSV | Imediato (1 comando) |
| **R2** Restauração seletiva | Apenas SES/SUSAM, SSP, SEJUSC, FENIXSOFT do backup | Cirúrgico |
| **R3** Migrar CSV permanente | Expandir esquema CSV (cnpj, evidencias_reconhecimento, modelo_notificacao, etc.) e abandonar JSON | Janela 3 |

**Recomendação minha: R1 agora + R3 como projeto Janela 3.**

### Decisão 3 — Validar 125 evidências SSP

Validar que as 125 evidências do `reconhecimento_revisado` do backup são adversarialmente sustentáveis (documentadas em PDFs originais, não só planilha). Critério: ≥60% tipo "E" ou "B". Requer PRODAM_DOCS local.

## Estado do roadmap (plano 2026-05-27)

| Janela | Estado em 28/05 |
|---|---|
| 0 — Imediata 72h | ✅ Fechada |
| 1 — Primeira semana | 1.4 ✅, 1.7 ✅, 1.8 ✅, **1.2 ✅ (28/05)**; 1.1/1.3/1.5/1.6 ⏸ (gate humano ou PRODAM_DOCS) |
| 2 — Protocolar + bugs | **2.4 ✅ (28/05)**; 2.0/2.2/2.6/2.7/2.8 ⏸ |
| 3 — Automação | 3.1 ✅, 3.3 ✅; 3.2/3.4-3.10 ⏸ |
| 4 — Higiene contínua | Não iniciada |

## Os 3 commits da sessão 28/05 — diff resumido

### `f8290de` — Janela 2.4 (D1 float→Decimal + DRY)

```
 scripts/auditoria_completude_devedor.py            |   4 +-   ← float()→brl() em empenhos_valor/faturas_valor
 scripts/dossie_multiformato_devedor.py             |  20 +--  ← helpers locais removidos, importa de prodam_utils
 tests/test_prodam_utils.py                         |  16 ++   ← +2 testes funcionais (sql_sum, fmt_brl aceita Decimal)
 tests/test_regression_decimal_dry.py               |  94 ++   ← NOVO: 6 lints estruturais (regex)
 _QUESTOES_CRITICAS/10_CONSOLIDAR_BRL_FMT_BRL_STRICT|  80 ++   ← NOVO: follow-up refatorar prodam_utils.fmt_brl strict
```

Bug colateral resolvido: D3 (except: nu) removido do dossie. Escopo cirúrgico: 3 de 26 float() totais — 23 restantes ficam fora (Chart.js JSON em dossie lines 145-185, detalhamento_faturas.py).

### `11d0c40` — Janela 1.2 (Auditoria reversão drift)

```
 _QUESTOES_CRITICAS/03_REVERSAO_DRIFT_20260513.md   | 175 +++   ← NOVO: análise forense + 3 decisões pendentes
```

4 siglas materialmente divergentes (backup 12/05 vs CSV atual):
- **SES/SUSAM**: +R$ 9,96M val_exig / +R$ 35,69M val_atualizado / +62 fat_exig (reversão de reconciliação validada)
- **SSP**: +15 fat_exig sem mudança de val; **perdeu campo `evidencias_reconhecimento: 125`** (fundamento Art. 202 VI CC)
- **SEJUSC**: -4 fat_exig (sutil)
- **FENIXSOFT**: 5 prescritas viraram exigíveis no CSV sem justificativa documental

Perda informacional: JSON tinha **128 campos por devedor**; CSV tem **11 colunas**. Perdidos: cnpj, evidencias_reconhecimento, modelo_notificacao, regime_execucao, score_composto, ev_valor_esperado, val_*_anterior.

### `cb61891` — /simplify (13 findings aplicados)

```
 _QUESTOES_CRITICAS/03_REVERSAO_DRIFT_20260513.md   |  10 +-   ← 3 erros factuais corrigidos (sign FENIXSOFT, p_recuperacao, Anexos removido)
 _QUESTOES_CRITICAS/10_CONSOLIDAR_BRL_FMT_BRL_STRICT|   2 +-   ← critério #4 ajustado (disclaim 7 outros scripts)
 scripts/dossie_multiformato_devedor.py             |   1 -    ← comentário Regra #16 redundante removido
 tests/test_prodam_utils.py                         |  22 +    ← teste duplicado reduzido + xfail fronteira Decimal
 tests/test_regression_decimal_dry.py               | 101 ±    ← regex → AST refactor (4 bugs) + parametrize + standalone runner
```

Sweep angle achou 4 bugs nos regex (truncation no `,`, multi-line falha, except: ignorado, MULTILINE limitado) — substituídos por AST walk.

## Issues abertas em `_QUESTOES_CRITICAS/`

| # | Título | Status | Pendência |
|---|---|---|---|
| 03 | Reversão drift 13/05 | needs-human-decision | Decisões 1, 2, 3 (Gabriel) |
| 06 | FENIXSOFT2 investigação | needs-info | 4 queries em `PRODAM_DOCS/_ANALISE/prodam.db` local |
| 10 | Consolidar brl/fmt_brl strict | needs-triage | Refactor `prodam_utils.fmt_brl` + DRY nos 7 outros scripts |

## Próximos passos (em ordem de prioridade)

1. **Gabriel decide D1 + D2 da Issue 03** (na máquina local com PRODAM_DOCS) — destrava item 1.6
2. **Item 2.2** triagem dos 18 SEM_d_plus (R$ 6,58M no limbo) — sem gate, 100% acionável em cloud
3. **Item 1.6** envio TRD SES/SUSAM — após D1
4. **Issue 10** refactor `prodam_utils.fmt_brl` strict-Decimal + DRY nos 7 scripts

## Estado técnico

- **Branch**: `claude/clever-bell-nhsDa` == `main` (`cdaa7b5`) — consolidação concluída (ver "Estado atual" no topo)
- **Working tree**: limpo
- **Tests**: 113 passed + 1 xfailed (xpass-on-Issue-10)
- **Ruff E9/F63/F7/F82**: limpo
- **Pre-commit**: ativo
- **PRODAM_DOCS**: gitignored — indisponível em sessões cloud

## Skills/comandos úteis para retomar

| Comando/Skill | Quando usar |
|---|---|
| `/compact` (built-in) | Compactar histórico desta sessão antes de finalizar |
| `/simplify` | Code review max-effort + apply fixes (gastei nesta sessão — funcionou bem) |
| `/code-review ultra` | Multi-agent cloud review (billed) |
| `/sincronizar-prodam` | Rebuild DB + audit + dossiê + CLAUDE.md (requer PRODAM_DOCS local) |
| `/loop <intervalo> <prompt>` | Vigia recorrente — bom para: prescrição diária, babysit CI, drift CSV↔CLAUDE.md, citação Teori em arquivos novos, TRDs parados, watch _QUESTOES_CRITICAS |
| Agent `adversarial-meta-auditor` | Gate jurídico obrigatório antes de TRD/notificação/dossiê |
| Agent `arquiteto-agents-prodam` | Meta-arquitetura do ecossistema |
| Agent `general-purpose` | Tarefas multi-step com tools amplos |
| Agent `Explore` | Read-only search rápido |

## Como retomar (PowerShell na máquina local)

```powershell
cd C:\Users\gabri\Desktop\PROJETO_PRODAM
git fetch origin
git checkout claude/jolly-heisenberg-mK6VU
git pull origin claude/jolly-heisenberg-mK6VU

# Ler o estado:
notepad .continue-here.md
notepad _QUESTOES_CRITICAS\03_REVERSAO_DRIFT_20260513.md

# Validar:
py -3.12 -m pytest tests\ -v   # esperado: 113 passed + 1 xfailed
git status                      # esperado: clean
git log --oneline HEAD~3..HEAD  # esperado: cb61891, 11d0c40, f8290de

# Aplicar Decisão R1 (se escolhida) — restaura profiles.json a partir do backup:
Copy-Item PRODAM_DOCS\profiles.json PRODAM_DOCS\profiles.json.bak.before-restore-20260528
Copy-Item _BACKUPS\profiles_PRE-REVERSAO-DRIFT-20260513-155115.json PRODAM_DOCS\profiles.json
py -3.12 scripts\auto_update_claude_md.py
git diff CLAUDE.md profiles_resumo.csv   # revisar antes de commit
```

## Referências da sessão anterior (Rota B 12/05) ainda válidas

- `scripts/auditoria_DE/rota_b_analise.py` — análise cruzada faturas D- × empenhos (foi a base para a auditoria 1.2)
- `PRODAM_DOCS/_QUESTOES_CRITICAS/auditoria_DE_2026-05-12/` — relatórios S1.1 (local-only)
- `_BACKUPS/profiles_PRE-REVERSAO-DRIFT-20260513-155115.json` — **backup canônico**, 334 KB, fonte de verdade da reconciliação 12/05
- `DOCUMENTOS_GERADOS/SES_SUSAM/TRD_SES_SUSAM_2026-05-12.docx` — TRD pronto, aguarda D1

## Insights estratégicos da sessão (para a próxima Claude session ler)

1. **CSV não é SSOT real** — é índice resumido. JSON era a fonte verdadeira, hoje só preservado em `_BACKUPS/`. Decisões jurídicas finais não podem ser tomadas só com base no CSV — toda decisão "PRESCRITA" precisa ser cruzada com o JSON antes de virar peça.

2. **`auto_update_claude_md.py` é all-or-nothing (bug D4)** — provável causa raiz da reversão de drift 13/05. Janela 2 item 2.6 propõe refactor com tags `<!-- manual:start --> ... <!-- manual:end -->`.

3. **Multa contratual R$ 500/dia + 10% por prescrição perdida** já incide. Memorando de exposição (R$ 10,46M) entregue ao Brandão Ozores em commit `71d81b0`.

4. **23 devedores já prescritos** (R$ 104,64M atualizado) **+ 1 crítico iminente** (ADS, R$ 211,7K) = **24 em risco** (alertas GitHub #6/#7). CLAUDE.md atual ainda diz só SSP em 40 dias — está desincronizado.

5. **A "completude documental" é o gargalo maior** — mediana 54,5% entre os 70 devedores, só DETRAN passou de 80%. Cada decisão "PRESCRITA" pode ser revertida por marco interruptivo escondido em PDF não-ingerido.

6. **`/loop` é o que faltou em 12/05-27/05** — ADS escapou 14 dias sem ninguém olhar o CSV. Próxima sessão deve considerar setup de `/loop 12h` para vigia de prescrição até a Janela 3.1 (cron diário) estar em produção.
